<%= form_with(model: wish_list, local: true) do |form| %>
  <% if wish_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wish_list.errors.count, "error") %> prohibited this wish_list from being saved:</h2>

      <ul>
      <% wish_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= form.label :user_id %><br />
    <%= form.select :user_id, @user_choice %>
  </div>


  <div class="field">
    <%= form.label :product_id %><br />
    <%= form.select :product_id, @product_choice %>
  </div>


  <div class="field">
    <%= form.label :dateadded %>
    <%= form.date_select :dateadded, id: :wish_list_dateadded %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
